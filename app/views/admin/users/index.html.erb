<h1>ユーザー管理</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col"><%= User.human_attribute_name(:name) %></th>
      <th scope="col"><%= User.human_attribute_name(:email) %></th>
      <th scope="col"><%= User.human_attribute_name(:task_count) %></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.tasks.size %></td>
        <td><%= link_to '詳細', admin_user_path(user) %></td>
        <td><%= link_to '編集', edit_admin_user_path(user) %></td>
        <td><%= link_to '削除', admin_user_path(user), method: :delete, data: { confirm: t('popup.confirm') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @users %>
