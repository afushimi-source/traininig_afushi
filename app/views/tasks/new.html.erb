<style>
  form { margin-bottom: 30px }
</style>

<h1>タスク作成</h1>

<%= render 'form', task: @task %>

<%= render 'labels/form', label: @label %>

<%= link_to 'タスク一覧へ戻る', tasks_path(take_params), class: 'btn btn-info' %>
